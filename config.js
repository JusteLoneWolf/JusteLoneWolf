module.exports = {
    GITHUB_TOKEN:secrets.GITHUB_TOKEN,
    WEATHER_TOKEN: secrets.WEATHER_TOKEN,
    CITY_ID: secrets.CITY_ID,
    TCK:secrets.TCK,
    TCS:secrets.TCS,
    TAC:secrets.TAC,
    TATS:secrets.TATS
}